export const siteConfig = {
  name: "OpportunityFlow",
  description:
    "Discover and track funding opportunities tailored to your startup.",
  url: process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000",
} as const;
